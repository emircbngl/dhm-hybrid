# Faz 1.1 (v1.0.1-ux) — Durum Notu · 2026-04-21

## Paydaşlar

**Lindqvist Lab (saha pilotu)**
- **Anna** — pilot kullanıcı. Faz 1 UX review'ını açan kişi. Altı şikâyetin listesi onun oturum notlarından geldi: dil karışımı, sessiz recon/QPI, geçip giden transient hata mesajları, parameter amnezisi, klavye yokluğu, Esc ile cancel yokluğu.
- **Lindqvist Lab ekibi** — DHM reconstruction pipeline'ını günlük iş akışında kullanan grup (günlük kullanıcı nezdinde "lab" dediğimiz bu).

**Bizim taraf (geliştirici)**
- **Emir** — kurucu geliştirici, onay ve plan noktası. "Onay senindir" kararlarını sen veriyorsun.
- **Claude** (AI asistanı) — plan uygulama, kod yazma, test, doğrulama. Self-improvement lessons loop'unu tutan taraf.

---

## Planlanan Fazlar

### Faz 1 — v1.0.0 (2026-04-20) · ŞİPLİ
Teknik review'ı geçti. Lab pilotunda **hem teknik hem görsel UX** sorunuyla geri döndü.

### Faz 1.1 — v1.0.1-ux · AKTİF
İki tier strateji:

**Tier 0 — Invisible Plumbing** (~8 gün, Tier 1'in altına mimari temel)
- `T0.1` progress protocol (`ProgressEvent`, `Operation`, `PhaseHandle`)
- `T0.2` structured error bus (`ErrorEvent`, `Severity`, `ErrorCenter`)
- `T0.3` settings schema v2 + `QSettings` store + v1→v2 migrator
- `T0.4` reconstruction_worker 7-faz refactor (preprocess → offaxis → freqfilter → propagate → refsub → finitecheck → unwrap)
- `T0.5` QPI + autofocus worker structured error/progress signals
- `T0.6` command registry + `QAction`/`QShortcut` migration

**Tier 1 — Visible Design** (~4 gün, Tier 0'ı tüketen görünen yüz)
- `T1.1` command palette (⌘K)
- `T1.2` toast + error drawer
- `T1.3` silent 1px progress line (<500ms gizli, >5s caption)
- `T1.4` parameter persistence (startup apply, post-run save, QFileDialog default path'leri)
- `T1.5` Türkçe → İngilizce dil temizliği + CI grep guard
- `T1.6` global Esc = cancel
- `T1.7` context-sensitive sidebar modes (stretch)

### Faz 2 — İleri (başlamadı)
AI segmentasyon, licensing, PDF rapor.

---

## Son Durum (bu oturum sonu)

| #           | Görev                      | Durum                     |
| ----------- | -------------------------- | ------------------------- |
| T0.1–T0.6   | Tüm plumbing               | ✅ shipped                 |
| T0 verif.   | Tier 0 doğrulama           | ✅ 107/107 yeşil           |
| T1.1        | Command palette            | ✅ shipped                 |
| T1.2        | Toast + drawer             | ✅ shipped                 |
| T1.3        | Progress line              | ✅ shipped                 |
| **T1.4**    | **Parameter persistence**  | ✅ **bu turda shipped**    |
| T1.5        | Dil + CI guard             | ⏳ sırada                 |
| T1.6        | Global Esc                 | ⏳ pending                |
| T1.7        | Context sidebar            | ⏳ stretch                |

**Test durumu**: `pytest` 137/137 yeşil (≈1.5s). Bu turda eklenen dokuz persistence testi dahil.

**Bu turda kapatılan T1.4 çıktıları**:
- Yeni modül `src/gui/persistence.py` — tabs ↔ `AppSettings` map'i, Qt'den bağımsız test edilebilir.
- `main_window.py`'da üç lifecycle hook: startup `apply`, recon/AF/QPI başarılarından sonra `save`, her `QFileDialog` teyidinden sonra I/O history merge.
- Altı dialog site'ı `last_folder`'a bağlandı; rapor dialog'u ayrıca `last_report_folder`'a.
- **Collateral fix**: `QShortcut` import'u `QtWidgets` → `QtGui` (PySide6 6.x); headless smoke test bunu yakaladı, `tasks/lessons.md`'ye kural olarak eklendi.

**Sıradaki adım**: T1.5 — `main_window.py`'daki 10+ Türkçe string'i İngilizce'ye çevir, dialog başlıklarını kısalt ("Görüntüyü dışa aktar…" → "Export image…"), `.pre-commit-config.yaml`'a `grep` guard ekle ki ileride Türkçe bir string commit'lenemesin.
