"""Track C — hybrid CNN reference-free reconstruction.

This package implements the residual-corrector network that takes the
output of the classical reffree pipeline (poly5 background fit) and
predicts the structured aberration residual the CNN should subtract.
"""
